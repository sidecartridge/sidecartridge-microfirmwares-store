# Local-only epics backlog (tracked on disk, not in git)
docs/

# Editor / OS cruft
.DS_Store

# Local Claude Code config stays out; the shared skill ships with the repo.
# `.claude/*` rather than `.claude/` so git still descends into skills/.
.claude/*
!.claude/skills/

# Python
__pycache__/
*.pyc
